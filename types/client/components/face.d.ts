/**
 * Build the DOM for a face of the field.
 *
 * @param {Array<Array<number>>} points
 * @param {'left' | 'right' | 'top'} direction
 * @returns {Array<*>}
 */
export function buildFace(points: Array<Array<number>>, direction: 'left' | 'right' | 'top'): Array<any>;
