/** @typedef {import('../state/initial').State} State */
/**
 * Build the data structure for svg that displays the player on the field.
 *
 * @param {State} state
 * @returns {Array<*>}
 */
export function buildPlayers(state: State): Array<any>;
export type State = import('../state/initial').State;
