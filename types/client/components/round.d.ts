/** @typedef {import('../state/initial').State} State */
/**
 * Build the data structure for el that displays current round information.
 *
 * @param {State} state
 * @returns {Array<*>}
 */
export function buildRound(state: State): Array<any>;
export type State = import('../state/initial').State;
